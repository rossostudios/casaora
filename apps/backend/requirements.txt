fastapi==0.128.2
# This repo currently uses a Python 3.9 venv by default.
# Some "latest" pins require Python 3.10+, so we pin to the latest Python 3.9-compatible versions.
uvicorn[standard]==0.39.0
pydantic-settings==2.11.0
python-dotenv==1.2.1
supabase==2.27.3
